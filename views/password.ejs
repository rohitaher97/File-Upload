<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/navbar.css">

    <title>Document</title>
  </head>
  <body>
    <nav>
      <h1> File Upload</h1></nav>
      <div class="container">
    <% if (locals.error) { %>
    <div style="margin-bottom: 1rem; color: red">Incorrect Password</div>
    <% } %>
    <h1 style="text-align: center;margin-bottom: 10px;">Download File</h1>

    <div
      style="
        display: grid;
        gap: 0.5rem;
        grid-template-columns: auto 1fr;
        max-width: 500px;
        margin: 20px auto;
      "
    >
      <%= locals.name %>
    </div>
    <form
      method="POST"
      style="
        display: grid;
        gap: 0.5rem;
        grid-template-columns: auto 1fr;
        max-width: 500px;
        margin: 0 auto;
      "
    >
      <% if (locals.name != null) { %> <% } %>
      <label for="password">Password:</label>
      <input type="password" id="password" name="password" required />
      <button style="grid-column: span 2" type="submit">Download</button>
    </form>
  </div>
  </body>
</html>
